# from django.conf.urls import url
import caja.views

urlpatterns = [
    # url(
    #     r'empresa/(?P<empresa>[0-9]+)/areas_moderadas/$',
    #     empresa.views.AreaModerarList.as_view(),
    #     name='areas-moderar-list'
    # ),
]